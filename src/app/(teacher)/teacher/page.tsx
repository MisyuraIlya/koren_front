"use client"
import React from 'react';
import Auth from '@/modules/auth';
import { Box } from '@mui/material';

const page = () => {
    return (
      <Box sx={{width:'60%'}}>
            teacher page
      </Box>
    );
};

export default page;