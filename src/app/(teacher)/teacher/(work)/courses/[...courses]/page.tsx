'use client'
import React from 'react';

interface CoursePageProps {
    params: {
        courses: string[]
    }
    searchParams: any
}

  
const page = () => {
    return (
        <div>
            asd
        </div>
    );
};

export default page;