import React from 'react';
import { Box } from '@mui/material';

const page = () => {
    return (
      <Box sx={{width:'60%'}}>
            student pages
      </Box>
    );
};

export default page;